using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NodePlayerScript : MonoBehaviour
{
    public int Health;
    public int corruptionLevel;
    public bool hasChainsaw;
    public bool hasPlasmaGun;
    public bool hasAssaultRifle;
    // Start is called before the first frame update
    void Start()
    {
        Health = 10;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
